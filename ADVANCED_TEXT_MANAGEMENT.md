# إدارة النصوص المتقدمة على صورة البطل

## نظرة عامة
تم إضافة نظام شامل للتحكم الكامل في جميع النصوص على صورة البطل (Hero) مع خيارات تنسيق متقدمة وتأثيرات ديناميكية.

## الميزات الرئيسية

### 1. التحكم الكامل في أنماط النصوص
- **حجم الخط**: من 16px إلى 96px
- **سمك الخط**: عادي، غامق، فائق الغموق
- **اللون**: اختر أي لون باستخدام منتقي الألوان
- **المحاذاة**: يسار، وسط، يمين
- **التحويل**: بدون تحويل، أحرف كبيرة، أحرف صغيرة، استقرار
- **المسافات**: تحكم دقيق بين الأحرف وارتفاع السطر

### 2. تأثيرات الظل والنصوص
- تفعيل/تعطيل الظل النصي
- تخصيص لون الظل
- التحكم في ضبابية الظل (blur)
- تحديد إزاحة الظل (X, Y offsets)

### 3. الكلمات الدوارة (Rotating Words)
- **أنواع الرسوم المتحركة**:
  - Fade (تلاشي)
  - Slide (انزلاق)
  - Flip (قلب)
  - Zoom (تكبير)
  - Typewriter (كاتبة)
  - Bounce (ارتداد)

- **السرعة**: من 1000ms إلى 10000ms
- **اللون**: اختر اللون المفضل
- **الخلفية**: تفعيل خلفية ملونة
- **الحشوة**: تحكم في المسافات حول النص
- **زوايا مستديرة**: تخصيص انحناء الزوايا

### 4. الأزرار (Buttons)
- اللون الأساسي
- لون عند التمرير (Hover)
- حجم الخط
- سمك الخط
- لون النص

## واجهات التحكم

### موقع الأداة
`Admin Dashboard > إدارة البطل > تبويبة "تنسيق النصوص المتقدم"`

### الأقسام المتاحة

#### 1. العنوان الرئيسي (Title)
التحكم الكامل في مظهر العنوان الرئيسي للصفحة.

```
القسم: "العنوان الرئيسي"
- حجم الخط: منزلق
- سمك الخط: قائمة منسدلة
- اللون: منتقي ألوان
- التحويل: قائمة منسدلة
- الظل النصي: تبديل + خيارات الظل
- زر إعادة تعيين: لاستعادة القيم الافتراضية
```

#### 2. الكلمات الدوارة (Rotating Words)
التحكم في الكلمات التي تتغير تلقائياً.

```
القسم: "الكلمات الدوارة"
- نوع الرسوم المتحركة: قائمة منسدلة
- السرعة: منزلق (1000-10000ms)
- اللون: منتقي ألوان
- زر إعادة تعيين: لاستعادة القيم الافتراضية
```

## المعاينة الفورية

جميع التغييرات تظهر مباشرة في معاينة البطل في الجانب الأيمن:
- تحديث حي للنصوص
- عرض الكلمات الدوارة المتعاقبة
- تطبيق الأنماط الديناميكية فوراً

## حفظ البيانات

جميع الإعدادات تُحفظ تلقائياً في Firebase Firestore:
- البيانات آمنة ومشفرة
- يتم المزامنة مع الصفحة الأمامية على الفور
- النسخ الاحتياطية التلقائية

## بنية البيانات

### TextStyle
```typescript
interface TextStyle {
  fontSize: number;
  fontWeight: "normal" | "bold" | "900";
  color: string;
  textAlign: "left" | "center" | "right";
  textShadow: boolean;
  shadowColor: string;
  shadowBlur: number;
  shadowOffsetX: number;
  shadowOffsetY: number;
  lineHeight: number;
  letterSpacing: number;
  textTransform: "none" | "uppercase" | "lowercase" | "capitalize";
  opacity: number;
}
```

### RotatingWordsStyle
```typescript
interface RotatingWordsStyle {
  animation: "fade" | "slide" | "flip" | "zoom" | "typewriter" | "bounce";
  speed: number; // milliseconds
  textColor: string;
  backgroundColor?: string;
  showBackground: boolean;
  paddingX: number;
  paddingY: number;
  borderRadius: number;
}
```

## الخيارات الافتراضية

- **حجم الخط (Title)**: 56px
- **سمك الخط**: bold
- **لون النص**: #ffffff (أبيض)
- **محاذاة**: center
- **الظل**: مفعل، أسود، ضبابية 8px
- **السرعة (Rotating)**: 3000ms
- **لون الكلمات الدوارة**: #FFD700 (ذهبي)

## نصائح الاستخدام

1. **للتصاميم الحديثة**: استخدم الألوان الزاهية والظلال الضخمة
2. **للتصاميم الكلاسيكية**: استخدم الألوان الكلاسيكية والظلال الخفيفة
3. **للتوازن**: حاول جعل جميع النصوص بنفس أسلوب العائلة (Font Family)
4. **الأداء**: قلل سرعة الكلمات الدوارة للحصول على تأثير أفضل

## الأسئلة الشائعة

### هل يمكن إضافة خطوط مخصصة؟
نعم، يمكن إضافة خطوط عبر Google Fonts في ملف CSS.

### هل يمكن تطبيق الأنماط على الصفحات الفرعية؟
حالياً الأنماط متاحة فقط للصفحة الرئيسية (Hero). يمكن توسيعها لاحقاً.

### ما هي أفضل ممارسة لحجم الخط؟
استخدم أحجام متدرجة: عنوان 48-64px، عنوان فرعي 24-32px، نصوص عادية 16-18px.

## الدعم والمساعدة

لأي مشاكل أو استفسارات، يرجى التواصل مع فريق الدعم.
